#include <stdio.h>

int main() {

    int distance;                           // Integer distance variable without initial declaration

    scanf("%d", &distance);                 // Reads the distance and stores it in the distance variable

    printf("%d minutos\n", distance*2);     // Prints the time in minutes on the screen

    return 0;
}